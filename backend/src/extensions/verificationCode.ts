import { Prisma } from "../generated/prisma/client";

export const verificationCodeExtension = Prisma.defineExtension({
  // client: {},
  query: {},
  model: {},
  result: {},
});

export default verificationCodeExtension;
